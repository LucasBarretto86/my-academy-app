name: Actions

on: push

jobs:
  Linters:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Markdown
        uses: avto-dev/markdown-lint@v1
        with:
          config: ".github/workflows/lints/.markdownlintrc"
          args: "*.md docs/*.md docs/**/*.md"
          ignore: "README.md"

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.3.0"
          bundler-cache: true

      - name: Rubocop
        run: bundle exec rubocop -f github

  CI:
    runs-on: ubuntu-latest
    env:
      RAILS_ENV: ${{env.test.RAILS_ENV}}
      DATABASE_URL: ${{env.test.DATABASE_URL}}
      JWT_SECRET_KEY: ${{env.test.JWT_SECRET_KEY}}
      ALLOWED_ORIGIN: ${{env.test.ALLOWED_ORIGIN}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.3.0"
          bundler-cache: true

      - name: Brakeman
        run: bundle exec brakeman -q -w2

      - name: Prepare Database
        run: |
          bin/rails db:create
          bin/rails db:schema:load
          bin/rails db:migrate RAILS_ENV=test

      - name: Run RSpec
        run: bundle exec rspec
